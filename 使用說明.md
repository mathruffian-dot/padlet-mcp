# padlet-mcp 使用說明

> 讓 AI Agent（Claude Code / Claude Desktop / Cursor…）直接操作 Padlet 的 MCP Server。
> 你只要對 AI「說話」，它就會開牆、貼卡、留言、出投票、讀結果。

---

## 一、它是什麼？

```
你對 AI 說一句話
      ↓
AI Agent（Claude Code / Desktop / Cursor）
      ↓  MCP 協定
padlet-mcp（本工具）
      ↓  Padlet 官方 API
你的 Padlet 版子
```

七個工具一覽：

| 工具 | 一句話說明 |
|------|-----------|
| `create_board` | 一句話生成全新 Padlet（AI 生成，含區段與初始卡片） |
| `get_board` | 讀整面牆：標題、區段、所有卡片、所有留言 |
| `create_post` | 貼卡片：文字、顏色、URL 附件、**poll 投票** |
| `create_comment` | 在卡片下留言 |
| `add_reaction` | 加愛心／星等／評分／投票 |
| `get_attachment` | 讀卡片附件：照片檔案網址、**poll 即時票數** |
| `whoami` | 驗證 API key、查目前帳號 |

---

## 二、安裝

### 事前準備：取得 Padlet API Key（一次性，約 1 分鐘）

1. 開啟 [padlet.com/dashboard/settings](https://padlet.com/dashboard/settings)
2. 左側選 **Developer（開發者）**
3. 點 API key 旁的 **Generate**，複製金鑰（`pdltp_` 開頭）

> ⚠️ 需要 Padlet **付費訂閱**才能產生 API key。
> ⚠️ API 只能操作你是 **admin / owner** 的版子。
> 🔐 金鑰等同密碼，不要貼到公開場合、不要進 git。

### 安裝方式

**🚀 一鍵安裝（推薦）——自動偵測 Claude Code / Claude Desktop / Codex / OpenCode / Antigravity**

```bash
npx -y padlet-mcp setup
```

精靈會詢問你的 API key，自動偵測本機裝了哪些 agent，逐一寫入正確格式的設定（TOML/JSON 自動判斷），改動前自動備份。已有 padlet 設定的 agent 會跳過不覆蓋。

選用參數：
- `--key <金鑰>`：直接帶入，跳過互動詢問
- `--dry-run`：預演模式，只顯示會改哪些 agent、不寫入
- `--local`：註冊本機絕對路徑（開發或尚未發布 npm 時用）

裝完重啟各 agent，對任何一個說「用 padlet 的 whoami 確認連線」即可驗證。

**手動安裝（單一 agent）：**

**A. Claude Code（一行指令）**

```bash
claude mcp add --scope user padlet -e PADLET_API_KEY=你的金鑰 -- npx -y padlet-mcp
```

**B. Claude Desktop**

設定檔 `claude_desktop_config.json` 加入：

```json
{
  "mcpServers": {
    "padlet": {
      "command": "npx",
      "args": ["-y", "padlet-mcp"],
      "env": { "PADLET_API_KEY": "你的金鑰" }
    }
  }
}
```

**C. Cursor / 其他 MCP client**：同上 JSON 片段（標準 stdio MCP）。

> 💡 需要 Node.js 18+。`npx -y` 每次自動抓最新版，永遠不用手動更新。
> 💡 尚未發布 npm 前，把 `npx -y padlet-mcp` 換成 `node <你的路徑>/padlet-mcp/index.js`。

### 安裝後第一句話

> 「用 padlet 的 whoami 確認連線」

看到你的帳號名稱 = 安裝成功。

---

## 三、七個工具的用法（含對話範例）

### 1. `whoami` — 連線檢查

| 參數 | 說明 |
|------|------|
| （無） | |

> 💬 「檢查 padlet 連線」

回傳帳號 ID、名稱、email、username。

### 2. `create_board` — 一句話生成新牆

| 參數 | 必填 | 說明 |
|------|------|------|
| `instructions` | ✅ | 自然語言建板指令（最多 2000 字），越具體越好：對象、科目、要哪些 section、每區放什麼 |
| `role` | — | 你的角色，預設 `teacher` |

> 💬 「幫我開一面國中數學『一元一次方程式』課前討論牆，分四個 section：迷思澄清、生活應用、挑戰題、自由提問，每區放 2 個引導問題，繁體中文」

- 非同步生成，工具會自動等到完成（約 30~90 秒），回傳**新牆網址**和 **QR code 圖片網址**
- AI 會連卡片顏色都分好區
- ⚠️ **生成的牆預設「關閉」留言與 reactions**——要用 `create_comment` / `add_reaction` 前，先到牆的設定手動開啟

### 3. `get_board` — 讀整面牆

| 參數 | 必填 | 說明 |
|------|------|------|
| `board` | ✅ | **直接貼 Padlet 網址**或 board ID |
| `raw` | — | `true` 回傳完整原始 JSON（預設精簡摘要，省 token） |

> 💬 「讀這面牆 https://padlet.com/xxx/padlet-xxxx，總結學生都在問什麼」

回傳：牆標題＋網址、所有 section（ID＋標題）、所有卡片（ID、標題、內文、顏色、所屬區段）、所有留言。

### 4. `create_post` — 貼卡片

| 參數 | 必填 | 說明 |
|------|------|------|
| `board` | ✅ | Padlet 網址或 board ID |
| `subject` | △ | 卡片標題 |
| `body` | △ | 卡片內文 |
| `attachment_url` | △ | 附件公開網址（圖片/影片/YouTube/PDF/網頁，Padlet 自動生成預覽） |
| `attachment_caption` | — | 附件說明文字 |
| `poll_question` | △ | 投票問題 |
| `poll_choices` | △ | 投票選項，如 `["x=3","x=4","x=5"]` |
| `section` | — | **直接寫區段名稱**（如「挑戰題」），工具自動比對；找不到會列出現有區段 |
| `color` | — | `red` / `orange` / `green` / `blue` / `purple` |

（△：標題、內文、附件、投票至少要有一個）

> 💬 「在挑戰題區貼一張紅色卡片：解 3(x-2)+5 = 2x+9」
> 💬 「出一題投票貼到自由提問區：2x+3=11 的解是多少？選項 x=3、x=4、x=5、x=7」
> 💬 「把這張圖 https://...png 貼到生活應用區，說明文字寫『畢氏定理示意圖』」

- ❌ 不能直接上傳本機檔案——檔案要先有公開網址（見第四節圖床做法）

### 5. `create_comment` — 卡片下留言

| 參數 | 必填 | 說明 |
|------|------|------|
| `post_id` | ✅ | 卡片 ID（用 `get_board` 查） |
| `html` | ✅ | 留言內容，支援 HTML |

> 💬 「對每張答錯的卡片留一句蘇格拉底式提問，不要直接給答案」

- ⚠️ 牆必須先開啟留言功能，否則回 `COMMENTS_NOT_ALLOWED`

### 6. `add_reaction` — 加 reaction

| 參數 | 必填 | 說明 |
|------|------|------|
| `post_id` | ✅ | 卡片 ID |
| `type` | — | `like`（讚）/ `star`（0-5 星）/ `grade`（0-100 分）/ `vote`（±1），預設 like |
| `value` | — | like 固定 1；star 0-5；grade 0-100；vote 1 或 -1 |

> 💬 「答對的卡片全部加愛心」
> 💬 「幫每張卡片的解題過程打分數（grade）」

- ⚠️ 牆必須先開啟對應的 reaction 類型，否則回 `Reactions not enabled`

### 7. `get_attachment` — 讀附件＋投票結果

| 參數 | 必填 | 說明 |
|------|------|------|
| `post_id` | ✅ | 卡片 ID |

> 💬 「讀出那題投票現在的票數分布」
> 💬 「下載這張卡片上學生拍的解題照片來批改」

回傳兩種資料：
- **URL 附件**：Padlet CDN 快取網址＋嵌入碼（學生上傳的照片由此取得）
- **Poll**：問題、每個選項的即時票數、投票是否進行中

---

## 四、實戰食譜（教學場景）

### 食譜 1：一句話開課全家桶

> 「開一面『XX 單元』討論牆（四區段），幫每區生一張插圖貼上，出 3 題迷思投票，最後把牆的 QR code 給我。」

流程：`create_board` → 生圖（如 gpt-image-2）→ 推圖床 → `create_post` 附圖 → `create_post` 出投票 → 回傳 QR code 網址（建牆時就附在回應裡）。

### 食譜 2：AI 批改產線（收作業神器）

學生把解題過程**拍照貼牆**（學生不用任何帳號設定），然後：

> 「讀這面牆，把每張學生照片的解題過程批改一遍，在卡片下留個別回饋，正確的加愛心。」

流程：`get_board` 列卡片 → `get_attachment` 拿照片網址 → AI 視覺辨識批改 → `create_comment` ＋ `add_reaction`。

### 食譜 3：形成性評量即時儀表板

> 「出 5 題本單元的投票，錯誤選項要混入真實迷思。」
> （學生投完後）「讀全部投票結果，告訴我哪個迷思最嚴重，下一堂課要補什麼。」

### 食譜 4：AI 助教即時回饋（課堂現場）

搭配 Claude Code 的 `/loop`：

> 「/loop 每 60 秒檢查這面牆，新的學生答案：答對加 ❤️，有迷思的留一句引導式提問，不要直接給答案。」

投影幕上的牆會「自己動起來」——卡片冒出、愛心亮起、AI 留言出現。

### 圖床做法（讓 AI 生成的圖能上牆）

Padlet API 不能上傳本機檔案，解法是 GitHub 公開 repo 當圖床：

1. 建一個公開 repo（如 `padlet-assets`），圖片放 `images/`
2. `git push` 後，網址格式：`https://raw.githubusercontent.com/<帳號>/<repo>/master/images/<檔名>.png`
3. 用 `create_post` 的 `attachment_url` 附上
4. Padlet 會把圖快取到自家 CDN，之後圖床的檔案搬移也不會破圖

---

## 五、API 能力邊界（先知道，不踩坑）

| 做不到的事 | 替代方案 |
|------------|----------|
| 刪除、編輯既有卡片 | 回 Padlet 介面手動 |
| 在既有牆新增 section | 開新牆時用指令描述讓 AI 一次生好 |
| 刪牆、改牆設定 | 回 Padlet 介面手動 |
| 上傳本機檔案 | 先放圖床拿公開網址（見上節） |
| 操作別人的牆 | 只能操作自己是 admin/owner 的牆 |

---

## 六、常見錯誤對照表

| 錯誤訊息 | 原因 | 解法 |
|----------|------|------|
| `401` API key 無效 | 金鑰打錯或過期 | 重新 Generate 並更新設定 |
| `403` 沒有權限 | 你不是該牆的 admin/owner | 換自己的牆 |
| `COMMENTS_NOT_ALLOWED` | 牆未開留言 | 牆設定 → 開啟留言 |
| `Reactions not enabled` | 牆未開 reactions | 牆設定 → 開啟對應 reaction 類型 |
| `404` 找不到資源 | board/post ID 錯 | 用 `get_board` 重查；牆 ID 也可從牆的「⋯ → Developer info」取得 |
| `429` rate limit | 請求太頻繁 | 稍等再試；批次操作放慢節奏 |
| `500`（get_attachment） | 個別附件 Padlet 處理失敗 | 跳過該附件，屬 Padlet 端個案 |
| 啟動就跳出 API key 教學框 | 環境變數沒設 | 檢查 MCP 設定的 `env` 欄位 |

---

## 七、FAQ

**Q：免費版 Padlet 能用嗎？**
A：不行，產生 API key 需要付費訂閱。但「學生」端不用任何訂閱——他們只是正常貼卡片。

**Q：board ID 在哪？**
A：通常直接貼牆的網址就行（工具會自動解析）。解析失敗時：打開牆 → 右上「⋯」→ Developer info → 複製 API ID。

**Q：AI 一次能貼幾張卡？**
A：沒有硬性限制，但注意 rate limit，大量貼卡時讓 AI 放慢（每張間隔 0.5 秒以上）。

**Q：學生資料安全嗎？**
A：本工具只是你的電腦與 Padlet 官方 API 之間的橋，資料不經過任何第三方伺服器。API key 存在你本機的 MCP 設定中。

---

*由 [mathruffian-dot/padlet-mcp](https://github.com/mathruffian-dot/padlet-mcp) 維護・MIT License*
