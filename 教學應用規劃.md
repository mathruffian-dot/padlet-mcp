# padlet-mcp 教學應用規劃

> 建立 2026-08-13 ・ 最後更新 2026-08-13
> 目標：做一面「padlet-mcp 教學應用」示範牆（含安裝教學），並開發四項教學應用技能。

---

## 零、事實查證結果（依 [官方 API 文件](https://docs.padlet.dev/)）

### ❌→✅ 查證 1：原本判定的「`create_board` 白名單 bug」**不存在**（已更正）

**原判定（錯的）**：官方文件把 AI Recipe 的輪詢網址記載成 `https://padlet.dev/api/public/v1/ai-recipe-boards/status/...`，而 Codex 的白名單只放行 `api.padlet.dev` + `/v1/`，據此推論 `create_board` 會被自己的安全檢查擋掉。

**實測（2026-08-13，真金鑰）**：真實 statusUrl 是

```
https://api.padlet.dev/v1/ai-recipe-boards/status/<id>
```

**完全符合 Codex 的原始白名單。是官方文件的範例過時了，`create_board` 從來沒壞過。**

白名單已改成雙 origin 允許清單，這個改動現在的定位是**防禦性保留**（萬一 Padlet 改用文件寫的網址就不會突然壞掉），而不是修 bug。`lib.js` 與測試的註解已更正，若要收緊白名單，`padlet.dev/api/public/v1` 那組可以拿掉。

> 教訓：官方文件的範例網址不等於實際行為，涉及外部 API 的推論要實測才算數。

### ✅ 查證 2：本機檔案確實不能上傳——PPT / PDF / Word 也不行

`add-post` 文件原句：**"Currently only supports url links and poll attachments."** `attachment` 只有 `url` / `caption` / `poll` 三個欄位，全 API 沒有任何 multipart、binary 或 presigned upload 端點。

**結論：不只圖片，PPT／PDF／Word／影片一律無法從本機直接上傳。**唯一路徑是「先取得公開網址 → `attachment.url`」。

### ⚠️ 查證 3：`get_attachment` 拿不到「原始檔」，限制項目 4

官方回傳只有 `previewImageUrl`、`embedCode`、`poll`（問題／各選項票數／是否進行中／關閉時間），**沒有原始上傳檔的下載網址**。

- **照片**：`previewImageUrl` 本身是圖，餵 Claude 視覺批改堪用（6/11 已驗證）
- **影片 / PDF / Word**：沒有文件保證的取得路徑 → **「下載學生影片分析」可能做不到**
- `post.content.attachment.url` 在學生上傳情境是否回 CDN 網址，文件沒寫 → **必須實測**

> 已把 `attachment` 加入 `get_board` 的回傳（見查證 4），實測時直接就看得到。

### ✅ 查證 4：`create_post` 補上五個官方欄位 —— **已實作（v0.2.0）**

| 新增參數 | 對應 API 欄位 | 教學應用 |
|---|---|---|
| `custom_fields` | `customFields` | ⭐ 評分／規準等級寫成結構化資料，`get_board` 讀回可直接統計 |
| `after_post_id` | `manualSortPosition.previousPostId` | ⭐ 教材照頁序排，不被建立時間打亂 |
| `status` | `status` | 學生互評前先送審；教材提前排程放出 |
| `canvas_props` | `canvasProps` | canvas 版面排概念圖／心智圖 |
| `map_props` | `mapProps` | map 版面做地理／田野調查 |

同時 `get_board` 補上讀回：卡片的 `attachment`、`status`、`sortIndex`、`customFields`，牆的 `settings`、`customFields`。
（`custom_fields` 只能寫、不能讀回等於半個功能；`settings` 則是自動偵測「留言／reactions 是否開啟」的關鍵。）

`subject` 上限 500 字、`body` 上限 10000 字，已加入驗證。

---

## 一、整體架構

```
                  ┌──────────────────────────────────┐
                  │  教學應用示範 Padlet（對外門面）   │
                  │  安裝教學 ＋ 四個技能演示區        │
                  └──────────────────────────────────┘
                                 ▲ 用自己做示範
   ┌─────────────────────────────┴─────────────────────────────┐
   │                    padlet-mcp v0.2.0                      │
   └─────────────────────────────┬─────────────────────────────┘
        ┌──────────────┬─────────┴──────────┬─────────────────┐
        ▼              ▼                    ▼                 ▼
   Skill 1        Skill 2              Skill 3           Skill 4
   範本建牆       教材上牆              自動回覆          繳交分析
```

依賴關係：**Skill 1 → Skill 2 →（Skill 3 ‖ Skill 4）**

---

## 二、Phase 0：實測探底（開工前置）

**測試牆已建立**：<https://padlet.com/mathruffian/padlet-mcp-phase-0-hh8ifa3bdccexve2>
board ID `hh8ifa3bdccexve2`（16 字元）・QR code `https://assets.padletcdn.com/padlets/hh8ifa3bdccexve2/qr_code.png`

> 💡 **不需要 Claude Code 註冊 padlet MCP 也能跑實測**：`PADLET_API_KEY` 已是使用者環境變數，
> 直接用 JSON-RPC 餵 `node index.js` 就能呼叫工具，全程不需要取得金鑰明文。
> 這也是最理想的測法——測到的是**我們自己的程式碼路徑**，不只是裸 API。

| # | 任務 | 狀態 |
|---|------|------|
| 0-1 | `resolveApiUrl` 白名單 | ✅ 判定錯誤已更正，白名單改為防禦性保留 |
| 0-2 | 真金鑰實跑 `create_board`，記錄真實 statusUrl | ✅ 成功，statusUrl = `api.padlet.dev/v1/ai-recipe-boards/status/<id>` |
| 0-2b | AI Recipe 是否忠實照抄區段規格 | ✅ 已測，見下方「區段規格實測結果」 |
| 0-3 | `board.settings` 能否偵測留言／reactions | 🔴 **不行**——實測只回 `{font, colorScheme}`，沒有互動設定 |
| 0-4 | 以學生身分上傳照片／影片／PDF／Word 各一 | ⬜ **需你手動操作**（樣本檔已備妥） |
| 0-5 | 對 0-4 每張卡片跑 `get_board` + `get_attachment` | ⬜ **決定項目 4 範圍** |
| 0-6 | `customFields` 定義與寫入格式 | 🟡 新牆的 `board.customFields` 是 `{}`，需你先在版子設定建欄位才能續測 |
| 0-7 | 50+ 卡片的大牆是否截斷／分頁 | ⬜ |

### 區段規格實測結果（0-2b）——比預期樂觀

用 Skill 1 的正式規格（參考資料／投票互動／留言互動／教材 2 段／備用 1-5，共 10 段）實測：

| 檢查項 | 結果 |
|--------|------|
| 區段**數量** | ✅ 精準 10 個 |
| 區段**順序** | ✅ 完全正確 |
| 區段**標題** | ❌ 全部被加上編號前綴，變成「1. 參考資料」而不是「參考資料」 |
| 每段初始卡片 | ✅ 各 1 張，共 10 張 |

**標題錯誤是我的指令造成的**——我用了「1. 參考資料 / 2. 投票互動 …」的編號清單，AI 把編號一起抄進標題。修正方式：規格清單不要編號，並明確寫「標題不得包含編號或符號」。

**這讓「難題 1」的風險大幅下降**：AI Recipe 對數量與順序是可靠的，只有標題字面需要嚴格約束＋驗收。

### 🔴 順帶抓到我們自己的一個缺陷（已修）

API 回傳的 `included` 陣列順序**不等於**版面顯示順序（實測 10 個 section 有兩處不同），而 `summarizeBoard` 既沒回傳 `sortIndex` 也沒排序 → **區段規格驗收會誤判順序錯亂**。已修：sections 與 posts 都改為依 `sortIndex` 排序，sections 也帶出 `sortIndex` 欄位。

---

## 三、教材與圖片上傳：全部走 Cloudflare 可行嗎？

**可以，但要用兩種服務，而且有一個前提要先確認。**

### 官方事實

| | Cloudflare Pages | Cloudflare R2 |
|---|---|---|
| 免費公開網址 | ✅ `<專案>.pages.dev`，**不需自有網域** | ⚠️ `pub-xxx.r2.dev` 但官方明說「rate-limited，僅供開發用途」 |
| 正式用途需自有網域 | ❌ 不需要 | ✅ **需要**（網域須掛在 Cloudflare） |
| 單檔上限 | **25 MiB** | 實質無上限 |
| 數量上限 | 單次部署 20,000 檔、每月 500 次 build | 無 |
| 免費額度 | — | 10 GB 儲存、Class A 100 萬 / Class B 1000 萬次／月 |
| 出流量費 | 免費 | **永久免費**（含 r2.dev） |
| 私有存取 | ❌ 公開 | ✅ 私有 bucket ＋簽章網址 |

### 建議分流

| 內容 | 放哪 | 理由 |
|------|------|------|
| 教材圖片、PDF 轉出的頁面圖（<25MiB） | **Cloudflare Pages** | 免費公開網址、不需網域，且能與項目 4 的成果網站**共用同一個 Pages 專案**（放 `/assets/`） |
| 大檔影片、大 PDF（>25MiB） | **R2**（或 YouTube 不公開連結） | Pages 塞不下 |
| 學生作品、個資、未公開試題 | **R2 私有 bucket ＋簽章網址** | 絕不可進公開空間 |

### ✅ Cloudflare 帳號實查結果（2026-08-13）

用本機已登入的 wrangler OAuth 權杖唯讀查詢（`zone:read` 權限有，結果可信）：

| 項目 | 實際狀況 |
|------|----------|
| 帳號 | `mathruffian@gmail.com`（單一帳號，ID `b87e54ac…`） |
| **網域 zones** | **0 個** ← 這個帳號下沒有任何自有網域 |
| Pages 專案 | **9 個**，全部用 `*.pages.dev`，無自訂網域（`eco-01-decision-making`、`math809-*` 系列） |
| R2 | 查不到——權杖的 scope **完全沒有任何 r2 權限** |

> 使用者原本認為有網域掛在 Cloudflare，但實查是 0 個。可能是：①網域在另一個 Cloudflare 帳號（不同 email）②網域註冊在別處但沒實際加進 Cloudflare ③把 9 個 `*.pages.dev` 誤認為自有網域。**待使用者確認。**

### 這對規劃的影響：幾乎沒有

因為決策本來就是「教材走 Pages」，而 Pages 不需要自有網域：

- ✅ **教材圖片／PDF → Pages**：完全不受影響，而且這台機器的 wrangler 已登入、9 個專案證明流程是活的
- ✅ **學生作品 → R2 私有 bucket ＋簽章網址**：簽章網址走 S3 API 端點，**不需要自有網域**
- ⚠️ **只有「>25MiB 的公開大檔」會卡住** → 走 YouTube 不公開連結
- ⚠️ **要用 R2 得先做兩件事**：①重新 `wrangler login` 授權含 r2 的 scope ②Cloudflare 帳號啟用 R2（即使免費額度也需先在後台完成啟用）

### 兩個實務要點

1. **Padlet 只快取「圖片」**：圖片附上後 Padlet 存一份到自家 CDN，圖床搬走也不破圖（6/11 驗證）。但 **PDF 不快取**，連結會一直被打——這正是 PDF 要放 Pages（不限速）而非 r2.dev（限速）的原因。
2. **PPT／Word 學生點了不一定能看**：`.pptx` 網址通常變成下載而非預覽。**教材一律先轉 PDF → 再轉頁面圖**最保險，這步包進 Skill 2 自動做。

### GitHub `padlet-assets` 怎麼辦？

**保留、但不再新增。**現有的圖已被 Padlet 快取，沒有回頭搬的必要；新教材一律走 Cloudflare。這樣也自然解決了「公開 repo 不可逆」的隱私風險。

---

## 四、Skill 1：教學版 Padlet 範本技能（`padlet-teaching-board`）

### 區段規格（已確認）

| 順序 | 區段名稱 | 內容 |
|------|----------|------|
| 1 | 參考資料 | 固定：課程參考素材 |
| 2 | 投票互動 | 固定：poll 投票卡片 |
| 3 | 留言互動 | 固定：需要學生留言回應的卡片 |
| 4 ~ 3+N | （依教材命名） | 實際教材內容，N 由教材決定 |
| 末 5 個 | 備用 1 ／ 備用 2 ／ 備用 3 ／ 備用 4 ／ 備用 5 | 空白備用 |

總區段數 = **3 + N + 5**

### 🔴 兩個結構性難題

**難題 1：section 只能在建牆那一次生成，且是 AI 非確定性生成**

`create_board`（AI Recipe）是唯一能產生 section 的途徑，API **不能對既有牆新增 section**。所以 3+N+5 個區段的名稱與順序必須在一段指令裡講完，但同一段指令跑兩次結果可能不同。N 越大風險越高。

因應：**規格 → 建牆 → 驗收 → 修補**

```
1. 讀教材（本機檔案或 Obsidian 筆記），決定 N
2. 組建牆指令：講明總數，區段清單**不要編號**（實測 AI 會把編號抄進標題），
   並明確寫「標題不得包含編號、序號或任何符號，完全照抄」
3. create_board
4. ⭐ 驗收：get_board 比對區段標題與順序（sections 已依 sortIndex 排序）
5. 不符 → 回報差異，由使用者決定重建或手動改名（API 改不了，但不會默默錯下去）
6. create_post 貼入三個固定區段的內容與教材（用 after_post_id 保順序）
7. 輸出牆網址＋QR code ＋「留言／reactions 預設關閉」的提醒
```

> **Plan B**：若驗收失敗率過高，改做「母版牆」＋Padlet 介面手動複製版子——結構 100% 確定，但複製這步 API 做不到。

**難題 2：區段 3「留言互動」一開始是死的**

AI 生成的牆**預設關閉留言與 reactions**。這代表區段 3 的設計目的（學生留言）在建完牆的當下無法運作，Skill 3 也一起卡住。

因應：**0-3 實測結果是「無法自動偵測」**——`board.settings` 只回 `{font, colorScheme}`，沒有任何留言／reactions 的開關資訊。所以只剩兩條路：

1. Skill 1 最後一步**強制輸出提醒**（必做，成本最低）
2. 想真的確認，只能**主動試一次**：對某張卡片呼叫 `create_comment`，收到 `COMMENTS_NOT_ALLOWED` 就代表沒開。缺點是成功時會留下一則測試留言（API 刪不掉，得手動刪）→ 建議只在使用者明確要求「幫我確認留言開了沒」時才做

### 教材輸入（已確認：本機檔案或 Obsidian 筆記皆可）

需要兩個 adapter：

- **本機檔案**：掃資料夾 → 依副檔名分類
- **Obsidian 筆記**：讀 markdown → ⚠️ **需解析 `![[圖片.png]]` wikilink 並找出 vault 的 attachment 實體路徑**，一併走 Skill 2 上傳，否則牆上會出現壞連結

---

## 五、Skill 2：教材上牆（`padlet-material-upload`）

一句話交付：**「把這個資料夾／這篇筆記的教材上牆」**

```
1. 來源解析：本機資料夾 or Obsidian 筆記（含 wikilink 圖片）
2. 分類：圖片 / PDF / PPT / Word / 影片
3. 轉檔：PPT、Word → PDF → 每頁 PNG（教材用圖最保險）
4. ⛔ 公開性確認閘：列出將被公開的檔案，等使用者點頭才繼續
5. 上傳 Cloudflare Pages（wrangler），>25MiB 走 R2 或 YouTube
6. create_post 逐一附上，帶 caption、section 定位、after_post_id 保序
   （每張間隔 ≥0.5 秒避 rate limit）
```

---

## 六、Skill 3：自動回覆留言（`padlet-auto-feedback`）

前置硬性條件：牆必須先開啟留言。技能第一步就檢查（0-3 後可自動偵測）。

```
1. get_board 取得所有卡片與既有留言
2. 過濾已回覆過的卡片（避免重複）
3. 依回饋提示詞判讀：正確 / 部分正確 / 迷思 / 空白
4. create_comment 寫個別化回饋
5. 可選：正確的 add_reaction 加愛心；評分寫入 custom_fields
6. 節流：每張間隔 ≥0.5 秒
```

**待決**：

- **重複回覆防護**：API 不能編輯／刪除留言，回錯只能手動刪。建議留言尾端加隱形標記（`<!-- ai-feedback -->`）供下次比對
- **回饋策略分科**：數學（蘇格拉底式提問、不給答案）與作文（指出可改處）差很多 → 做成可切換 profile
- **自動輪詢**：`/loop` 是 Claude Code 限定，其他 agent 需手動重跑

---

## 七、Skill 4：學生繳交分析與呈現（`padlet-submission-report`）

**已確認：只做前端**——純靜態網站，無後端無資料庫。分析在 agent 端跑完，結果 bake 成靜態 HTML 再部署 Cloudflare Pages。

| 繳交型態 | 取得方式 | 可行性 |
|----------|----------|--------|
| 文字留言／卡片內文 | `get_board` | ✅ |
| 照片 | `previewImageUrl` → Claude 視覺批改 | ✅ 已驗證 |
| 投票 | `get_attachment` 的 poll 票數 | ✅ |
| PDF / Word | `attachment.url` 是否可下載 | ❓ 待 0-5 |
| 影片 | 同上，且需抽格 | ❓ 可能做不到 |

```
1. get_board 收集卡片、留言、投票（大牆分批，見 0-7）
2. 依型態取資料
3. 套評量規準逐件評分 → 寫回 custom_fields（0-6 確認後）
4. 產出：a. 教師端總覽（表現分布、最嚴重迷思、下堂課要補什麼）
         b. 學生端靜態頁
5. wrangler 部署 Cloudflare Pages
```

### ✅ 隱私處理：學生代號化（已確認）

「只做前端」解決了架構複雜度，但沒有解決公開性——Pages 預設有網址就看得到。**已決定採學生代號化**：

- 頁面**只出現座號／代號**（如「07 號」），不出現真名
- 卡片內文若含學生自己寫的姓名，**產出網站前要先過一道去識別化**（不能只靠「我們不主動寫姓名」）
- Padlet 的 author 欄位、照片中的手寫姓名同樣要處理——**照片是最容易漏的**，學生手寫作業常在紙上寫名字，貼上網站等於公開真名
- 對照表（代號 ↔ 真名）只留在教師本機，不進網站、不進 git

---

## 八、里程碑

| Phase | 內容 | 阻塞於 |
|-------|------|--------|
| 0 | 實測探底 0-2 ~ 0-7 | 0-4 需你手動上傳 |
| 1 | 教學應用示範 Padlet（安裝教學＋四技能演示區） | Phase 0 |
| 2 | Skill 1 範本建牆 | 0-2、0-3 |
| 3 | Skill 2 教材上牆 | Cloudflare 前提確認 |
| 4 | Skill 3 自動回覆 | Phase 2 |
| 5 | Skill 4 繳交分析＋Pages | 0-5、0-6、隱私決策 |

## 九、還缺的決定 / 阻塞點

1. 🔴 **padlet MCP 在這台電腦還沒安裝**（`~/.claude.json` 沒有 padlet 設定）→ agent 無法呼叫 Padlet 工具，**Phase 0 的 0-2 ~ 0-7 全部卡住**。
   解法：使用者在自己的終端機跑 `npx -y padlet-mcp setup`（帶新金鑰），重啟 Claude Code 後工具才會上線。
2. **Cloudflare 網域**：實查 0 個 zone，與使用者認知不符 → 請確認是否在另一個帳號，或其實沒有。（不影響教材走 Pages 的主線）
3. **R2 若要用**：需重新 `wrangler login` 授權 r2 scope ＋後台啟用 R2。
4. ✅ 已決定：學生頁**代號化**、Skill 4 **只做前端**、教材來源**本機或 Obsidian 皆可**、區段規格已定。

### Phase 0 樣本檔：已備妥

`phase0-samples/`（不進 git，產生器在 `tools/gen_phase0_samples.py`）：

| 檔案 | 型態 | 大小 |
|------|------|------|
| `01-學生照片-手寫解題.png` | 照片 | 60 KB |
| `02-學生影片-口說解釋.mp4` | 影片（5 秒，含音軌） | 94 KB |
| `03-學生報告.pdf` | PDF（2 頁） | 44 KB |
| `04-學生作業.docx` | Word | 36 KB |

照片與 Word 的解題**刻意寫錯**（移項沒變號：`2x+3=11` → `2x=11+3`），讓後續視覺批改測試有東西可抓。

---

*相關文件：[CLAUDE.md](CLAUDE.md)・[handoff.md](handoff.md)・[使用說明.md](使用說明.md)*
